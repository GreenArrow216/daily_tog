import { ImagesData } from "../constants";
import ImageFade from "./imageFade/imageFade";

const Gallery = ({ images }: { images: ImagesData[] }) => {
  return (
    <div>
      test
      {/* rewriting the component for more responsive friendly design */}
    </div>
  );
};

export default Gallery;
